Your connection to www.lab.ser is encrypted with 128-bit encryption.

The connection uses TLS 1.1.

The connection is encrypted using AES_128_CBC, with SHA1 for message
authentication and ECDHE_RSA as the key exchange mechanism.